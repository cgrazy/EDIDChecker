using System;
using System.ComponentModel;
using System.Drawing;
using System.Globalization;
using Microsoft.Win32.SafeHandles;

namespace EDIDChecker
{
 /*       public struct Size
        {
            internal int cx;
            internal int cy;

            public int Width {
                get {return cx;}
            }
            
            public int Height {
                get{return cy;}
            }

            public Size(int width, int height)
            {
                cx = width;
                cy = height;
            }
            public Size(Size size)
            {
                cx = size.Width;
                cy = size.Height;
            }

        }
*/
/*        public class SizeF
        {
            Size size;
            
            public SizeF()
            {
                
            }
           
            public SizeF(Size newSize)
            {
                size = newSize;
            }
            public int Width {
                get {return size.Width;}
            }
            
            public int Height {
                get{return size.Height;}
            }


            public string ToString(CultureInfo culture)
            {
                return "SizeF - {Width=" + this.size.Width.ToString((IFormatProvider)culture) + ", Height=" + this.size.Height.ToString((IFormatProvider)culture) + "}";
            }

        }
*/
/*
         public class SizeFF
        {
            SizeF? size;
            
            public SizeFF()
            {
            }

            public SizeFF(SizeF newSize)
            {
                size = newSize;
            }

            public string ToString(CultureInfo culture)
            {
                return "SizeFF - {Width=" + this.size.Width.ToString((IFormatProvider)culture) + ", Height=" + this.size.Height.ToString((IFormatProvider)culture) + "}";
            }
        }
*/
}